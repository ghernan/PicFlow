# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PicFlow' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PicFlow
  pod 'FacebookLogin'
  pod 'TwitterKit'
  pod 'Google/SignIn'
  pod 'ObjectMapper', '~> 2.2'
  pod 'Alamofire', '~> 4.5'
  pod 'R.swift'
  pod 'AlamofireImage', '~> 3.1'

  target 'PicFlowTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PicFlowUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
